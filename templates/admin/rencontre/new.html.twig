{% extends 'admin/base_admin.html.twig' %}

{% block title %}Nouvelle Rencontre - Admin{% endblock %}

{% block body %}
<div class="admin-header">
    <h1>Nouvelle Rencontre</h1>
</div>
<div class="admin-content">
    {{ form_start(form) }}
        <div class="mb-3">
            {{ form_row(form.adversaire) }}
        </div>
        <div class="mb-3">
            {{ form_row(form.date) }}
        </div>
        <div class="mb-3">
            {{ form_row(form.lieu) }}
        </div>
        <div class="mb-3">
            {{ form_row(form.heure) }}
        </div>
        <button type="submit" class="btn btn-success">Créer</button>
        <a href="{{ path('admin_rencontre_index') }}" class="btn btn-secondary">Annuler</a>
    {{ form_end(form) }}
</div>
{% endblock %} 