<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Welcome!{% endblock %}</title>
        
        <style>
            /* Réinitialisation des styles de base pour assurer une cohérence entre les navigateurs */
            * { box-sizing: border-box; }
            
            /* Configuration de base pour le HTML et le body
               - Hauteur minimale de 100vh pour occuper tout l'écran
               - Police Arial par défaut
               - Fond transparent pour permettre l'affichage du fond personnalisé */
            html, body {
                min-height: 100vh;
                margin: 0;
                font-family: Arial, sans-serif;
                background: none;
                height: 100%;
            }
            
            body {
                min-height: 100vh;
                display: flex;
                flex-direction: column;
                justify-content: flex-start;
                height: 100%;
            }
            
            /* Barre de navigation principale
               - Largeur de 95% avec marges automatiques
               - Fond semi-transparent avec effet de flou
               - Bordure rouge et ombre portée pour la profondeur
               - Flexbox pour l'alignement des éléments
               - z-index élevé pour rester au-dessus du fond */
            .navbar {
                width: 95%;
                margin: 1.5rem auto 0 auto;
                background: none;
                border: none;
                border-radius: 0;
                box-shadow: none;
                display: flex;
                align-items: center;
                justify-content: space-between;
                padding: 0.5rem 1.2rem;
                position: relative;
                z-index: 10;
            }
            
            /* Configuration du logo
               - Hauteur fixe de 60px
               - Flexbox pour centrer verticalement
               - Image redimensionnée pour maintenir les proportions */
            .navbar-logo {
                display: flex;
                align-items: center;
                height: 60px;
            }
            .navbar-logo img {
                height: 100%;
                max-width: 120px;
                object-fit: contain;
            }
            
            /* Navigation principale
               - Espacement uniforme entre les liens
               - Transitions douces pour les interactions
               - Couleurs cohérentes avec le thème */
            .navbar-links {
                display: flex;
                gap: 1.2rem;
            }
            .nav-link {
                text-decoration: none;
                color: inherit;
                font-weight: 500;
                font-size: 1.1rem;
                padding: 0.3rem 0.8rem;
                border-radius: 0;
                transition: none;
            }
            .nav-link:hover {
                background: none;
                color: inherit;
            }
            
            /* Section des actions (recherche et connexion)
               - Alignement flexible des éléments
               - Styles cohérents pour les boutons
               - Transitions pour les interactions */
            .navbar-actions {
                display: flex;
                align-items: center;
                gap: 0.7rem;
            }
            .search-form {
                display: flex;
                align-items: center;
            }
            .search-input {
                border: none;
                border-radius: 0;
                background: none;
                color: inherit;
                font-size: 1rem;
                padding: 0.3rem 0.7rem;
                width: 120px;
            }
            .search-btn {
                border: none;
                border-radius: 0;
                background: none;
                color: inherit;
                font-size: 1.1rem;
                padding: 0.3rem 0.7rem;
                cursor: pointer;
                transition: none;
            }
            .search-btn:hover {
                background: none;
                color: inherit;
            }
            .auth-btn {
                border: none;
                border-radius: 0;
                background: none;
                color: inherit;
                font-size: 1rem;
                font-weight: 500;
                padding: 0.3rem 0.9rem;
                text-decoration: none;
                transition: none;
            }
            .auth-btn:hover {
                background: none;
                color: inherit;
            }
            
            .site-footer {
                width: 100%;
                background: none;
                border-top: none;
                margin-top: 1.2rem;
                padding: 0.5rem 0 0.3rem 0;
                font-size: 1rem;
                color: inherit;
                box-shadow: none;
                position: relative;
                z-index: 2;
                flex-shrink: 0;
            }
            .footer-container {
                max-width: 1400px;
                margin: 0 auto;
                padding: 0 1.2rem;
                width: 100%;
            }
            .footer-row {
                display: flex;
                flex-direction: row;
                align-items: center;
                justify-content: space-between;
                width: 100%;
                gap: 1.2rem;
            }
            .footer-col {
                flex: 1 1 0;
                display: flex;
                align-items: center;
                justify-content: center;
                min-width: 0;
            }
            .footer-brand {
                justify-content: flex-start;
                gap: 0.4rem;
                font-weight: 500;
            }
            .footer-logo {
                height: 28px;
                width: auto;
                border-radius: 0;
                background: none;
                border: none;
                padding: 0;
            }
            .footer-links {
                justify-content: center;
                gap: 0.7rem;
                flex-wrap: wrap;
            }
            .footer-links a {
                color: inherit;
                text-decoration: none;
                font-weight: 500;
                transition: color 0.18s;
                font-size: 1rem;
            }
            .footer-links a:hover {
                color: inherit;
                text-decoration: underline;
            }
            .footer-extra {
                justify-content: flex-end;
                color: inherit;
                font-size: 0.97rem;
                text-align: right;
            }
            @media (max-width: 900px) {
                .footer-container {
                    padding: 0 0.2rem;
                }
                .footer-row {
                    gap: 0.5rem;
                }
            }
            @media (max-width: 700px) {
                .footer-row {
                    flex-direction: column;
                    align-items: stretch;
                    gap: 0.5rem;
                }
                .footer-col {
                    width: 100%;
                    justify-content: center !important;
                    text-align: center;
                }
                .footer-brand {
                    flex-direction: column;
                    align-items: center;
                    gap: 0.2rem;
                }
                .footer-links {
                    flex-direction: column;
                    flex-wrap: nowrap;
                    align-items: stretch;
                    justify-content: center;
                    gap: 0.2rem;
                    margin-top: 0.2rem;
                    font-size: 0.95rem;
                    width: 100%;
                }
                .footer-links a {
                    display: block;
                    width: 100%;
                    min-width: 0;
                    max-width: 100%;
                    text-align: center;
                    white-space: normal;
                    padding: 0.35em 0;
                }
                .footer-extra {
                    font-size: 0.92rem;
                    text-align: center;
                    margin-top: 0.1rem;
                    width: 100%;
                }
            }
            .main-content {
                flex: 1 0 auto;
                display: block;
            }
            @media (max-width: 800px) {
                .navbar-links, .navbar-actions {
                    display: none;
                }
                .burger-new {
                    display: block;
                    background: none;
                    border: none;
                    font-size: 2.2rem;
                    color: inherit;
                    cursor: pointer;
                    z-index: 1101;
                    position: relative;
                }
                .burger-new span,
                .burger-new span:before,
                .burger-new span:after {
                    display: block;
                    background: #222;
                    height: 3px;
                    width: 32px;
                    border-radius: 2px;
                    position: absolute;
                    left: 0;
                    transition: 0.3s;
                    content: '';
                }
                .burger-new span {
                    position: relative;
                    top: 10px;
                }
                .burger-new span:before {
                    top: -10px;
                }
                .burger-new span:after {
                    top: 10px;
                }
                .burger-new.open span {
                    background: transparent;
                }
                .burger-new.open span:before {
                    transform: translateY(10px) rotate(45deg);
                }
                .burger-new.open span:after {
                    transform: translateY(-10px) rotate(-45deg);
                }
                .menu-overlay {
                    display: none;
                    position: fixed;
                    top: 0; left: 0; right: 0; bottom: 0;
                    width: 100vw; height: 100vh;
                    background: rgba(30,30,30,0.3);
                    z-index: 1100;
                    align-items: stretch;
                    justify-content: flex-end;
                }
                .menu-overlay.open {
                    display: flex;
                }
                .menu-drawer {
                    background: rgba(30,30,30,0.85);
                    backdrop-filter: blur(8px);
                    width: 70vw;
                    max-width: 340px;
                    height: 100vh;
                    box-shadow: -2px 0 16px rgba(0,0,0,0.18);
                    display: flex;
                    flex-direction: column;
                    align-items: center;
                    justify-content: center;
                    position: relative;
                    right: 0;
                    top: 0;
                    transition: transform 0.3s cubic-bezier(.4,0,.2,1);
                }
                .menu-overlay ul {
                    list-style: none;
                    padding: 0;
                    margin: 0;
                    text-align: center;
                    width: 100%;
                }
                .menu-overlay li {
                    margin: 1.5rem 0;
                }
                .menu-overlay a {
                    color: #fff;
                    font-size: 1.3rem;
                    font-family: 'Montserrat', Arial, sans-serif;
                    font-weight: 400;
                    letter-spacing: 1px;
                    text-decoration: none;
                    transition: color 0.2s;
                    cursor: pointer;
                }
                .menu-overlay a:hover {
                    color: #ff4b6e;
                }
            }
            @media (min-width: 801px) {
                .burger-new, .menu-overlay { display: none !important; }
            }
        </style>
    </head>
    <body>
        <!-- Barre de navigation principale
             - Structure flexible pour tous les écrans
             - Logo, liens et actions organisés -->
        <nav class="navbar">
            <a href="/" class="navbar-logo">
                <img src="/image/logopingcdj.png" alt="CDJ Villereau Logo">
            </a>
            <!-- Liens de navigation
                 - Structure sémantique
                 - Accessibilité préservée -->
            <div class="navbar-links">
                <a href="/accueil" class="nav-link">Accueil</a>
                <a href="/actualite" class="nav-link">Actualités du Ping</a>
                <a href="/entrainement" class="nav-link">Entrainement</a>
                <a href="/partenaire" class="nav-link">Partenaires</a>
                <a href="/adhesion" class="nav-link">Adhesion</a>
                <a href="/contact" class="nav-link">Contact</a>
                <a href="/avis" class="nav-link">Vos avis</a>
            </div>
            <!-- Actions de la barre de navigation
                 - Boutons d'action principaux
                 - Style cohérent avec le thème -->
            <div class="navbar-actions">
                <a href="/connexion" class="auth-btn">Connexion</a>
            </div>
            <button class="burger-new" id="burgerBtn" aria-label="Menu" type="button">
                <span></span>
            </button>
            <div class="menu-overlay" id="menuOverlay">
                <nav class="menu-drawer">
                    <ul>
                        <li><a href="/accueil">Accueil</a></li>
                        <li><a href="/actualite">Actualités du Ping</a></li>
                        <li><a href="/entrainement">Entrainement</a></li>
                        <li><a href="/partenaire">Partenaires</a></li>
                        <li><a href="/contact">Contact</a></li>
                        <li><a href="/adhesion">Adhesion</a></li>
                        <li><a href="/avis">Vos avis</a></li>
                        <li><a href="/connexion">Connexion</a></li>
                    </ul>
                </nav>
            </div>
        </nav>

        <!-- Contenu principal -->
        <div class="main-content" style="position:relative;z-index:1; flex:1 0 auto;">
            {% block body %}{% endblock %}
        </div>

        <!-- Footer légal et informatif -->
        <footer class="site-footer">
            <div class="footer-container">
                <div class="footer-row">
                    <div class="footer-col footer-brand">
                        <img src="/image/logopingcdj.png" alt="CDJ Villereau Logo" class="footer-logo">
                        <span>CDJ Villereau &copy; {{ "now"|date("Y") }}</span>
                    </div>
                    <nav class="footer-col footer-links">
                        <a href="/mentions-legales">Mentions légales</a>
                        <a href="/confidentialite">Politique de confidentialité</a>
                        <a href="/contact">Contact</a>
                        <a href="/accessibilite">Accessibilité</a>
                        <a href="/cookies">Gestion des cookies</a>
                    </nav>
                    <div class="footer-col footer-extra">
                        <span>Association loi 1901 - SIRET : [à compléter] - Siège : [adresse à compléter]</span>
                    </div>
                </div>
            </div>
        </footer>
        <script>
            const burgerBtn = document.getElementById('burgerBtn');
            const menuOverlay = document.getElementById('menuOverlay');
            burgerBtn.addEventListener('click', function() {
                burgerBtn.classList.toggle('open');
                menuOverlay.classList.toggle('open');
            });
            menuOverlay.addEventListener('click', function(e) {
                if(e.target === menuOverlay) {
                    burgerBtn.classList.remove('open');
                    menuOverlay.classList.remove('open');
                }
            });
        </script>
    </body>
</html>

